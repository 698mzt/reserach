# 多环境配置使用说明

## ✅ 配置完成

项目已成功配置多环境数据源，支持开发、测试、生产三个环境的数据库配置自动切换。

## 📁 配置文件说明

- `application-druid.yml` - 开发环境配置（开发时默认使用）
- `application-druid-test.yml` - 测试环境配置  
- `application-druid-prod.yml` - 生产环境配置

## 🚀 使用方式

### 1. 开发环境（默认）

在IDE中直接运行项目时，会自动使用 `application-druid.yml` 配置文件，连接开发数据库。

### 2. 打包部署

在IDEA右侧Maven面板中，可以在Profiles中选择对应的环境：

- **不选择任何Profile** → 使用开发环境配置（`application-druid.yml`）
- **选择test** → 使用测试环境配置（`application-druid-test.yml`）  
- **选择prod** → 使用生产环境配置（`application-druid-prod.yml`）

### 3. 命令行打包

```bash
# 打包开发环境（默认）
mvn clean package

# 打包测试环境
mvn clean package -Ptest

# 打包生产环境  
mvn clean package -Pprod
```

### 4. 使用批处理脚本打包

```bash
# 打包测试环境
package.bat test

# 打包生产环境
package.bat prod
```

## 🗄️ 数据库连接信息

- **开发环境**: 123.56.76.249:3309/2024keyan (1Q2W3E4R)
- **测试环境**: 123.56.76.249:3309/2024keyan (1Q2W3E4R)  
- **生产环境**: 39.105.158.38:3309/2024keyan (1q2w3e4r)

## ⚙️ 技术实现

1. **Maven Profiles**: 配置了dev/test/prod三个profile
2. **资源过滤**: 通过Maven资源过滤，将application.yml中的`${profile.active}`变量动态替换
3. **自动切换**: 打包时根据激活的profile自动选择对应的数据源配置文件

## ✨ 重要特性

- ✅ 开发时直接运行项目使用开发环境配置
- ✅ 打包时根据选择的Profile自动切换对应的数据源配置  
- ✅ 应用启动时无需额外指定profile参数，已内置到打包文件中
- ✅ 各环境的数据库连接配置完全分离，避免配置冲突
- ✅ 支持IDEA Maven面板可视化选择环境