# 八大模块接入清单

## 一、概述

本文档列出八大模块各自需要改造的 service、controller、list 页面、detail 页面、动作规则来源，形成统一的接入实施清单。八大模块为：**论文、教材软著、专利软著、讲座报告、奖励、横向项目、纵向项目、成果转化**。按照此清单逐项改造，即可完成各模块的页面渲染统一化接入。

---

## 二、后端接入方式（通用规则引擎）

新方案采用**通用规则引擎**，无需为每个模块单独编写状态加载方法和按钮规则方法。所有模块共用同一套按钮构造逻辑，仅通过 `ModuleConfig` 注册表区分模块编码、流程编码和权限前缀。

### 2.1 接入原理

`PageRenderServiceImpl` 中已实现通用按钮规则引擎，核心机制：

1. **模块注册表**：`MODULE_REGISTRY` Map 中已预注册八大模块的模块编码、权限前缀、流程编码、权限映射表
2. **状态映射加载**：`loadAllModuleConfigs()` 启动时自动从数据库加载所有已注册模块的状态映射和节点映射
3. **通用按钮规则**：`buildCommonActions()` 基于状态编码后缀（`_DRAFT`/`_JYS_AUDIT`/`_KYC_AUDIT`/`_PASSED`/`_REJECTED`）自动判断按钮显示规则
4. **权限动态解析**：通过 `config.getPermission(actionKey)` 动态获取各模块的实际权限标识，消除 if-else 判断
5. **模块特有按钮**：`addModuleSpecificActions()` 处理模块特有按钮（如横向课题的"提交结项申请"）

### 2.2 状态编码命名规范

所有模块的状态编码必须遵循统一后缀规则：

| 后缀 | 通用语义 | 颜色类型 | 示例状态编码 |
|------|---------|---------|-------------|
| `_DRAFT` | 草稿/待提交 | default（灰色） | REWARD_DRAFT, TEXTBOOK_DRAFT |
| `_JYS_AUDIT` | 教研室审批中 | warning（橙色） | REWARD_JYS_AUDIT, TEXTBOOK_JYS_AUDIT |
| `_KYC_AUDIT` | 科研处审批中 | info（青色） | REWARD_KYC_AUDIT, TEXTBOOK_KYC_AUDIT |
| `_PASSED` | 审批通过 | success（绿色） | REWARD_PASSED, TEXTBOOK_PASSED |
| `_REJECTED` | 审批驳回 | danger（红色） | REWARD_REJECTED, TEXTBOOK_REJECTED |

### 2.3 已注册模块清单

`PageRenderServiceImpl` 中 `MODULE_REGISTRY` 已注册的模块：

| 模块编码 | 权限前缀 | 流程编码 | 模块名称 |
|---------|---------|---------|---------|
| `PAPER` | `system:paper` | `PAPER_APPROVAL` | 论文 |
| `REWARD` | `system:reward` | `REWARD_APPLY` | 奖励 |
| `TEXTBOOK` | `system:jiaocairuanzhu` | `TEXTBOOK_APPROVAL` | 教材软著 |
| `PATENT` | `system:zhuanliruanzhu` | `PATENT_APPLY` | 专利软著 |
| `LECTURE` | `system:report` | `LECTURE_APPROVAL` | 讲座报告 |
| `HORIZONTAL_APPLY` | `system:apply` | `HORIZONTAL_APPLY` | 横向项目申报 |
| `HORIZONTAL_OVER` | `system:apply` | `HORIZONTAL_OVER` | 横向项目结题 |
| `VERTICAL_APPLY` | `system:apply_vertical` | `VERTICAL_APPLY` | 纵向项目申报 |
| `VERTICAL_OVER` | `system:apply_vertical` | `VERTICAL_OVER` | 纵向项目结题 |
| `TEC_TRA` | `system:intraSch` | `TEC_TRA_APPROVAL` | 成果转化 |

### 2.4 权限映射机制

各模块权限名称不同，通过 `ModuleConfig` 的权限映射表动态解析：

```java
// 示例：获取教研室审批权限
// 论文模块 -> system:paper:process
// 纵向模块 -> system:apply_vertical:JYS
// 成果转化 -> system:intraSch:JYPY
config.getPermission("approve")
```

**各模块权限映射表：**

| 模块 | approve (教研室) | kypy (科研处) | revoke (撤回) | kyrevoke (科研退回) |
|------|-----------------|--------------|--------------|-------------------|
| 论文 | process | kypy | revoke | kyrevoke |
| 奖励 | process | hecha | revoke | kyrevoke |
| 教材软著 | process | hecha | chexiao | kyrevoke |
| 专利软著 | process | hecha | chexiao | kyrevoke |
| 讲座报告 | process | check | revoke | kyrevoke |
| 横向项目 | process | hecha | revoke | kyrevoke |
| 纵向项目 | JYS | KYC | revoke | kyrevoke |
| 成果转化 | JYPY | KYPY | JYCH | KYCH |

### 2.5 旧状态码兼容

部分模块（教材软著、专利软著、成果转化）历史数据使用数字状态码，需要在 ServiceImpl 中添加 `mapStateToStatusCode()` 映射方法：

```java
/**
 * 将旧状态码（数字）映射为统一状态编码
 * 示例：教材软著模块旧状态码（0-7）映射为 TEXTBOOK_* 格式
 */
private String mapStateToStatusCode(String state) {
    if (state == null) return "TEXTBOOK_DRAFT";
    // 如果已经是新格式状态编码，直接返回
    if (state.contains("_")) return state;
    // 旧数字状态码映射
    switch (state) {
        case "0": return "TEXTBOOK_DRAFT";
        case "1": return "TEXTBOOK_JYS_AUDIT";
        case "2": case "4": return "TEXTBOOK_KYC_AUDIT";
        case "3": case "5": case "7": return "TEXTBOOK_REJECTED";
        case "6": return "TEXTBOOK_PASSED";
        default: return "TEXTBOOK_DRAFT";
    }
}
```

### 2.6 通用按钮规则

八大模块共用同一套按钮规则，由 `buildCommonActions()` 方法实现：

| 状态语义 | 按钮 | 显示条件 |
|---------|------|---------|
| 草稿（`_DRAFT`） | 查看、编辑、删除、提交、查看流程 | 创建人/管理员 + 对应权限 |
| 驳回（`_REJECTED`） | 查看、编辑、查看流程 | 创建人/管理员 + 对应权限 |
| 教研室审批（`_JYS_AUDIT`） | 查看、查看流程、批阅、通过、驳回 | 有 `config.getPermission("approve")` 权限 |
| 教研室审批（`_JYS_AUDIT`） | ~~撤回~~ | **不显示** |
| 科研处审批（`_KYC_AUDIT`） | 查看、查看流程、核查、通过、驳回 | 有 `config.getPermission("kypy")` 权限 |
| 科研处审批（`_KYC_AUDIT`） | 撤回 | 有 `config.getPermission("approve")` 权限（教研室审批人可撤回） |
| 通过（`_PASSED`） | 查看、查看流程 | 有 `config.getPermission("info")` 权限 |
| 通过（`_PASSED`） | 撤回（科研处） | 有 `config.getPermission("kyrevoke")` 权限 |

---

## 三、整体接入流程

### 3.1 后端接入步骤（每个模块通用）

| 步骤 | 改造内容 | 说明 |
|------|---------|------|
| 1 | 领域类添加 `statusMeta` 和 `actions` 字段 | 参照 `SciPaperA.java` 的字段定义 |
| 2 | ServiceImpl 添加 `fillPageRenderData()` 方法 | 设置 moduleCode、permPrefix、processCode，调用公共服务 |
| 3 | ServiceImpl 列表查询方法中调用 `fillPageRenderData()` | 在返回每条记录前调用填充方法 |
| 4 | ServiceImpl 详情查询方法中调用 `fillPageRenderData()` | 在返回详情前调用填充方法 |
| 5 | 旧状态码兼容（如需要） | 添加 `mapStateToStatusCode()` 方法 |

> **注意**：无需修改 `PageRenderServiceImpl`，通用规则引擎已自动支持所有已注册模块。

### 3.2 前端接入步骤（每个模块通用）

| 步骤 | 改造内容 | 说明 |
|------|---------|------|
| 1 | HTML 页面引入 `page-render.js` | 在页面 `<script>` 区域添加引入 |
| 2 | 改造列表页状态列渲染 | 使用 `PageRender.renderStatusToString(row.statusMeta)` |
| 3 | 改造列表页操作列渲染 | 使用 `PageRender.renderActionsToString(actions)` |
| 4 | 注册动作处理器 | 调用 `PageRender.registerHandlers()` 注册所需动作 |
| 5 | 改造详情页底部按钮 | 使用 `PageRender.renderActions()` |
| 6 | 保留原有表单提交逻辑 | 不改动原审批业务逻辑 |

---

## 四、论文模块（PAPER）

### 4.1 接入状态

| 分类 | 状态 | 说明 |
|------|------|------|
| 后端 | ✅ 已完成 | 已在 PageRenderServiceImpl 和 SciPaperAServiceImpl 中完成接入 |
| 前端 | ✅ 已完成 | 已在 paper.html 和 detail.html 中完成接入 |
| 数据库 | ✅ 已完成 | sys_approval_process、sys_approval_state、sys_approval_node 已配置 |

### 4.2 已改造文件清单

| 文件类型 | 文件路径 | 改造内容 |
|---------|---------|---------|
| 公共服务 | `PageRenderServiceImpl.java` | 实现通用规则引擎和权限映射 |
| 领域类 | `SciPaperA.java` | 新增 `statusMeta` 和 `actions` 字段 |
| Service | `SciPaperAServiceImpl.java` | 列表和详情查询调用 `fillPageRenderData()` |
| 列表页 | `paper.html` | 状态列和操作列改用 PageRender 渲染 |
| 详情页 | `detail.html` | 底部按钮改用 PageRender 渲染 |

---

## 五、奖励模块（REWARD）

### 5.1 接入状态

| 分类 | 状态 | 说明 |
|------|------|------|
| 后端 | ❌ 未接入 | 需添加 statusMeta/actions 字段和 fillPageRenderData() 方法 |
| 前端 | ❌ 未接入 | 待改造 |
| 数据库 | ✅ 已完成 | sys_approval_process、sys_approval_state、sys_approval_node 已配置 |

### 5.2 待改造文件清单

| 文件类型 | 文件路径 | 改造内容 |
|---------|---------|---------|
| 领域类 | `SysReward.java` | 新增 `statusMeta` 和 `actions` 字段 |
| Service | `SysRewardServiceImpl.java` | 添加 `fillPageRenderData()` 方法，列表和详情查询中调用 |
| 列表页 | `reward/reward.html` | 状态列和操作列改用 PageRender 渲染 |
| 详情页 | `reward/detail.html` | 底部按钮改用 PageRender 渲染 |

### 5.3 模块配置

| 配置项 | 值 |
|-------|-----|
| moduleCode | `REWARD` |
| permPrefix | `system:reward` |
| processCode | `REWARD_APPLY` |

### 5.4 权限映射

| 动作标识 | 实际权限后缀 | 完整权限标识 |
|---------|-------------|-------------|
| approve | process | `system:reward:process` |
| kypy | hecha | `system:reward:hecha` |
| revoke | revoke | `system:reward:revoke` |
| kyrevoke | kyrevoke | `system:reward:kyrevoke` |

---

## 六、教材软著模块（TEXTBOOK）

### 6.1 接入状态

| 分类 | 状态 | 说明 |
|------|------|------|
| 后端 | ❌ 未接入 | 需添加 statusMeta/actions 字段和 fillPageRenderData() 方法 |
| 前端 | ❌ 未接入 | 待改造 |
| 数据库 | ✅ 已完成 | sys_approval_process、sys_approval_state、sys_approval_node 已配置 |

### 6.2 待改造文件清单

| 文件类型 | 文件路径 | 改造内容 |
|---------|---------|---------|
| 领域类 | `SciJiaocairuanzhu.java` | 新增 `statusMeta` 和 `actions` 字段 |
| Service | `SciJiaocairuanzhuServiceImpl.java` | 添加 `fillPageRenderData()` 和 `mapStateToStatusCode()` 方法，列表和详情查询中调用 |
| 列表页 | `jiaocairuanzhu/jiaocairuanzhu.html` | 状态列和操作列改用 PageRender 渲染 |
| 详情页 | `jiaocairuanzhu/detail.html` | 底部按钮改用 PageRender 渲染 |

### 6.3 模块配置

| 配置项 | 值 |
|-------|-----|
| moduleCode | `TEXTBOOK` |
| permPrefix | `system:jiaocairuanzhu` |
| processCode | `TEXTBOOK_APPROVAL` |

### 6.4 权限映射

| 动作标识 | 实际权限后缀 | 完整权限标识 |
|---------|-------------|-------------|
| approve | process | `system:jiaocairuanzhu:process` |
| kypy | hecha | `system:jiaocairuanzhu:hecha` |
| revoke | chexiao | `system:jiaocairuanzhu:chexiao` |
| kyrevoke | kyrevoke | `system:jiaocairuanzhu:kyrevoke` |

### 6.5 特殊说明

教材软著模块存在旧状态码（0-7），需要添加 `mapStateToStatusCode()` 方法兼容映射为 `TEXTBOOK_*` 格式。

---

## 七、专利软著模块（PATENT）

### 7.1 接入状态

| 分类 | 状态 | 说明 |
|------|------|------|
| 后端 | ❌ 未接入 | 需添加 statusMeta/actions 字段和 fillPageRenderData() 方法 |
| 前端 | ❌ 未接入 | 待改造 |
| 数据库 | ✅ 已完成 | sys_approval_process、sys_approval_state、sys_approval_node 已配置 |

### 7.2 待改造文件清单

| 文件类型 | 文件路径 | 改造内容 |
|---------|---------|---------|
| 领域类 | `SciZhuanliruanzhu.java` | 新增 `statusMeta` 和 `actions` 字段 |
| Service | `SciZhuanliruanzhuServiceImpl.java` | 添加 `fillPageRenderData()` 和 `mapStateToStatusCode()` 方法，列表和详情查询中调用 |
| 列表页 | `zhuanliruanzhu/zhuanliruanzhu.html` | 状态列和操作列改用 PageRender 渲染 |
| 详情页 | `zhuanliruanzhu/detail.html` | 底部按钮改用 PageRender 渲染 |

### 7.3 模块配置

| 配置项 | 值 |
|-------|-----|
| moduleCode | `PATENT` |
| permPrefix | `system:zhuanliruanzhu` |
| processCode | `PATENT_APPLY` |

### 7.4 权限映射

| 动作标识 | 实际权限后缀 | 完整权限标识 |
|---------|-------------|-------------|
| approve | process | `system:zhuanliruanzhu:process` |
| kypy | hecha | `system:zhuanliruanzhu:hecha` |
| revoke | chexiao | `system:zhuanliruanzhu:chexiao` |
| kyrevoke | kyrevoke | `system:zhuanliruanzhu:kyrevoke` |

### 7.5 特殊说明

专利软著模块存在旧状态码（0-7），需要添加 `mapStateToStatusCode()` 方法兼容映射为 `PATENT_*` 格式。

---

## 八、讲座报告模块（LECTURE）

### 8.1 接入状态

| 分类 | 状态 | 说明 |
|------|------|------|
| 后端 | ❌ 未接入 | 需添加 statusMeta/actions 字段和 fillPageRenderData() 方法 |
| 前端 | ❌ 未接入 | 待改造 |
| 数据库 | ✅ 已完成 | sys_approval_process、sys_approval_state、sys_approval_node 已配置 |

### 8.2 待改造文件清单

| 文件类型 | 文件路径 | 改造内容 |
|---------|---------|---------|
| 领域类 | `SciLectureReport.java` | 新增 `statusMeta` 和 `actions` 字段 |
| Service | `SciLectureReportServiceImpl.java` | 添加 `fillPageRenderData()` 方法，列表和详情查询中调用 |
| 列表页 | `report/report.html` | 状态列和操作列改用 PageRender 渲染 |
| 详情页 | `report/detail.html` | 底部按钮改用 PageRender 渲染 |

### 8.3 模块配置

| 配置项 | 值 |
|-------|-----|
| moduleCode | `LECTURE` |
| permPrefix | `system:report` |
| processCode | `LECTURE_APPROVAL` |

### 8.4 权限映射

| 动作标识 | 实际权限后缀 | 完整权限标识 |
|---------|-------------|-------------|
| approve | process | `system:report:process` |
| kypy | check | `system:report:check` |
| revoke | revoke | `system:report:revoke` |
| kyrevoke | kyrevoke | `system:report:kyrevoke` |

---

## 九、横向项目模块（HORIZONTAL）

### 9.1 接入状态

| 分类 | 状态 | 说明 |
|------|------|------|
| 后端 | ❌ 未接入 | 需添加 statusMeta/actions 字段和 fillPageRenderData() 方法 |
| 前端 | ❌ 未接入 | 待改造 |
| 数据库 | ✅ 已完成 | sys_approval_process、sys_approval_state、sys_approval_node 已配置 |

### 9.2 待改造文件清单

| 文件类型 | 文件路径 | 改造内容 |
|---------|---------|---------|
| 领域类 | `SciHorizontalApply.java` | 新增 `statusMeta` 和 `actions` 字段 |
| Service | `SciHorizontalApplyServiceImpl.java` | 添加 `fillPageRenderData()` 方法，根据 state 前缀区分申报/结题流程 |
| 列表页 | `apply/apply.html` | 状态列和操作列改用 PageRender 渲染 |
| 详情页 | `apply/detail.html` | 底部按钮改用 PageRender 渲染 |

### 9.3 模块配置

| 配置项 | 值 |
|-------|-----|
| moduleCode（申报） | `HORIZONTAL_APPLY` |
| moduleCode（结题） | `HORIZONTAL_OVER` |
| permPrefix | `system:apply` |
| processCode（申报） | `HORIZONTAL_APPLY` |
| processCode（结题） | `HORIZONTAL_OVER` |

### 9.4 权限映射

| 动作标识 | 实际权限后缀 | 完整权限标识 |
|---------|-------------|-------------|
| approve | process | `system:apply:process` |
| kypy | hecha | `system:apply:hecha` |
| revoke | revoke | `system:apply:revoke` |
| kyrevoke | kyrevoke | `system:apply:kyrevoke` |

### 9.5 特殊说明

横向项目包含**申报**（HORIZONTAL_APPLY）和**结题**（HORIZONTAL_OVER）两条独立审批流程，需要在 `fillPageRenderData()` 中根据 `state` 前缀（`HORIZONTAL_APPLY_`/`HORIZONTAL_OVER_`）动态设置 processCode。

---

## 十、纵向项目模块（VERTICAL）

### 10.1 接入状态

| 分类 | 状态 | 说明 |
|------|------|------|
| 后端 | ❌ 未接入 | 需添加 statusMeta/actions 字段和 fillPageRenderData() 方法 |
| 前端 | ❌ 未接入 | 待改造 |
| 数据库 | ✅ 已完成 | sys_approval_process、sys_approval_state、sys_approval_node 已配置 |

### 10.2 待改造文件清单

| 文件类型 | 文件路径 | 改造内容 |
|---------|---------|---------|
| 领域类 | `SciHorizontalApplyVertical.java` | 新增 `statusMeta` 和 `actions` 字段 |
| Service | `SciHorizontalApplyVerticalServiceImpl.java` | 添加 `fillPageRenderData()` 方法，根据 state 前缀区分申报/结题流程 |
| 列表页 | `apply_vertical/apply.html` | 状态列和操作列改用 PageRender 渲染 |
| 详情页 | `apply_vertical/detail.html` | 底部按钮改用 PageRender 渲染 |

### 10.3 模块配置

| 配置项 | 值 |
|-------|-----|
| moduleCode（申报） | `VERTICAL_APPLY` |
| moduleCode（结题） | `VERTICAL_OVER` |
| permPrefix | `system:apply_vertical` |
| processCode（申报） | `VERTICAL_APPLY` |
| processCode（结题） | `VERTICAL_OVER` |

### 10.4 权限映射

| 动作标识 | 实际权限后缀 | 完整权限标识 |
|---------|-------------|-------------|
| approve | JYS | `system:apply_vertical:JYS` |
| kypy | KYC | `system:apply_vertical:KYC` |
| revoke | revoke | `system:apply_vertical:revoke` |
| kyrevoke | kyrevoke | `system:apply_vertical:kyrevoke` |

### 10.5 特殊说明

1. 纵向项目包含**申报**（VERTICAL_APPLY）和**结题**（VERTICAL_OVER）两条独立审批流程
2. 权限标识使用**大写**（JYS、KYC），与其他模块不同，需要在 `fillPageRenderData()` 中根据 `state` 前缀动态设置 processCode

---

## 十一、成果转化模块（TEC_TRA）

### 11.1 接入状态

| 分类 | 状态 | 说明 |
|------|------|------|
| 后端 | ❌ 未接入 | 需添加 statusMeta/actions 字段和 fillPageRenderData() 方法 |
| 前端 | ❌ 未接入 | 待改造 |
| 数据库 | ✅ 已完成 | sys_approval_process、sys_approval_state、sys_approval_node 已配置 |

### 11.2 待改造文件清单

| 文件类型 | 文件路径 | 改造内容 |
|---------|---------|---------|
| 领域类 | `SciIntraSchoolPro.java` | 新增 `statusMeta` 和 `actions` 字段 |
| Service | `SciIntraSchProApplyServiceImpl.java` | 添加 `fillPageRenderData()` 和 `mapStateToStatusCode()` 方法，列表和详情查询中调用 |
| 列表页 | `IntraSchPro/view.html` | 状态列和操作列改用 PageRender 渲染 |
| 详情页 | `IntraSchPro/detail.html` | 底部按钮改用 PageRender 渲染 |

### 11.3 模块配置

| 配置项 | 值 |
|-------|-----|
| moduleCode | `TEC_TRA` |
| permPrefix | `system:intraSch` |
| processCode | `TEC_TRA_APPROVAL` |

### 11.4 权限映射

| 动作标识 | 实际权限后缀 | 完整权限标识 |
|---------|-------------|-------------|
| approve | JYPY | `system:intraSch:JYPY` |
| kypy | KYPY | `system:intraSch:KYPY` |
| revoke | JYCH | `system:intraSch:JYCH` |
| kyrevoke | KYCH | `system:intraSch:KYCH` |

### 11.5 特殊说明

1. 成果转化模块复用 `SciIntraSchoolPro` 领域类，但使用独立的状态编码前缀 `TEC_TRA_*`
2. 需要添加 `mapStateToStatusCode()` 方法兼容旧状态码（1/2/3/4/5/12/15）

---

## 十二、接入进度汇总

| 模块 | 后端接入 | 前端接入 | 数据库配置 | 完成度 |
|------|---------|---------|-----------|--------|
| 论文模块 | ✅ | ✅ | ✅ | 100% |
| 教材软著模块 | ❌ | ❌ | ✅ | 33% |
| 奖励模块 | ❌ | ❌ | ✅ | 33% |
| 专利软著模块 | ❌ | ❌ | ✅ | 33% |
| 讲座报告模块 | ❌ | ❌ | ✅ | 33% |
| 横向项目模块 | ❌ | ❌ | ✅ | 33% |
| 纵向项目模块 | ❌ | ❌ | ✅ | 33% |
| 成果转化模块 | ❌ | ❌ | ✅ | 33% |

---

## 十三、接入验证清单

每个模块接入后需完成以下验证：

| 验证项 | 验证内容 | 通过标准 |
|--------|---------|---------|
| 列表页状态列 | 各状态在列表页的颜色和文案是否正确 | 与状态映射表一致 |
| 列表页操作列 | 各状态下按钮显示是否正确 | 与按钮规则表一致 |
| 详情页按钮 | 详情页底部按钮显示是否正确 | 与列表页操作列一致 |
| 角色差异 | 不同角色看到的按钮是否正确 | 与按钮规则表一致 |
| 驳回后重新提交 | 驳回状态下编辑后重新提交是否正常 | submit 按钮正确触发 |
| 撤回场景 | 各撤回场景按钮是否正确 | 与按钮规则表一致 |
| 颜色风格 | 状态颜色和按钮颜色是否与现有页面风格一致 | 无明显风格割裂 |
| 原审批流程 | 原有审批提交流程是否正常 | 不受改造影响 |

---

## 十四、接入实施步骤详解

### 14.1 后端实施步骤

#### 步骤 1：领域类添加字段

在领域类中添加 `statusMeta` 和 `actions` 字段：

```java
/** 状态展示元数据 */
private PageRenderStatusMeta statusMeta;

/** 按钮动作列表 */
private List<PageRenderActionItem> actions;

// 生成 getter 和 setter
public PageRenderStatusMeta getStatusMeta() { return statusMeta; }
public void setStatusMeta(PageRenderStatusMeta statusMeta) { this.statusMeta = statusMeta; }
public List<PageRenderActionItem> getActions() { return actions; }
public void setActions(List<PageRenderActionItem> actions) { this.actions = actions; }
```

#### 步骤 2：ServiceImpl 添加 fillPageRenderData() 方法

```java
@Autowired
private IPageRenderService pageRenderService;

/**
 * 填充页面渲染数据（状态展示和按钮动作列表）
 */
private void fillPageRenderData(YourDomainObject obj) {
    if (obj == null) return;
    
    SysUser currentUser = ShiroUtils.getSysUser();
    List<String> permissions = buildCurrentPermissions(currentUser);
    List<String> roleKeys = buildCurrentRoleKeys(currentUser);
    
    // 如果有旧状态码，需要映射为新状态编码
    String normalizedState = mapStateToStatusCode(obj.getState());
    
    PageRenderContext context = new PageRenderContext();
    context.setModuleCode("YOUR_MODULE_CODE");          // 模块编码
    context.setBusinessId(obj.getId());                  // 业务ID
    context.setCurrentState(normalizedState);            // 当前状态
    context.setCreatorId(obj.getUserId());               // 创建者ID
    context.setCurrentUser(currentUser);                 // 当前用户
    context.setPermissions(permissions);                 // 权限列表
    context.setRoleKeys(roleKeys);                       // 角色列表
    context.setPermPrefix("system:your_prefix");         // 权限前缀
    context.setProcessCode("YOUR_PROCESS_CODE");         // 流程编码
    
    PageRenderStatusMeta statusMeta = pageRenderService.buildStatusMeta(context);
    List<PageRenderActionItem> actions = pageRenderService.buildActions(context);
    
    obj.setStatusMeta(statusMeta);
    obj.setActions(actions);
}

/**
 * 构建当前用户权限列表
 */
private List<String> buildCurrentPermissions(SysUser user) {
    // 从数据库或缓存中获取用户权限
    // 返回权限列表，如 ["system:paper:info", "system:paper:edit", ...]
}

/**
 * 构建当前用户角色列表
 */
private List<String> buildCurrentRoleKeys(SysUser user) {
    // 从数据库或缓存中获取用户角色
    // 返回角色列表，如 ["admin", "jys", "ky", ...]
}
```

#### 步骤 3：列表查询中调用 fillPageRenderData()

```java
@Override
public List<YourDomainObject> selectYourList(YourDomainObject obj) {
    List<YourDomainObject> list = yourMapper.selectYourList(obj);
    // 为每条记录填充页面渲染数据
    for (YourDomainObject item : list) {
        fillPageRenderData(item);
    }
    return list;
}
```

#### 步骤 4：详情查询中调用 fillPageRenderData()

```java
@Override
public YourDomainObject selectYourById(Long id) {
    YourDomainObject obj = yourMapper.selectYourById(id);
    // 填充页面渲染数据
    fillPageRenderData(obj);
    return obj;
}
```

### 14.2 前端实施步骤

#### 步骤 1：引入 page-render.js

在 HTML 页面的 `<script>` 区域添加引入：

```html
<!-- 引入 page-render.js -->
<script th:src="@{/js/page-render.js}"></script>
```

#### 步骤 2：改造列表页状态列渲染

```javascript
// 原来：
{
    field: 'state',
    title: '状态',
    formatter: function(value, row, index) {
        // 手动判断状态
    }
}

// 改为：
{
    field: 'state',
    title: '状态',
    formatter: function(value, row, index) {
        return PageRender.renderStatusToString(row.statusMeta);
    }
}
```

#### 步骤 3：改造列表页操作列渲染

```javascript
// 原来：
{
    field: 'operate',
    title: '操作',
    formatter: function(value, row, index) {
        // 手动拼接按钮
    }
}

// 改为：
{
    field: 'operate',
    title: '操作',
    formatter: function(value, row, index) {
        return PageRender.renderActionsToString(row.actions);
    }
}
```

#### 步骤 4：注册动作处理器

```javascript
$(function() {
    // 注册动作处理器
    PageRender.registerHandlers({
        view: function(row) {
            // 查看详情
            $.modal.open("详情", ctx + "your/detail/" + row.id);
        },
        edit: function(row) {
            // 编辑
            $.modal.open("编辑", ctx + "your/edit/" + row.id);
        },
        submit: function(row) {
            // 提交审批
            $.modal.confirm("确定要提交该记录吗？", function() {
                $.operate.post(ctx + "your/submit", { id: row.id });
            });
        },
        approve: function(row) {
            // 通过
            $.modal.confirm("确定要通过该记录吗？", function() {
                $.operate.post(ctx + "your/approve", { id: row.id });
            });
        },
        reject: function(row) {
            // 驳回
            $.modal.confirm("确定要驳回该记录吗？", function() {
                $.modal.open("驳回", ctx + "your/reject/" + row.id);
            });
        },
        recall: function(row) {
            // 撤回
            $.modal.confirm("确定要撤回该记录吗？", function() {
                $.operate.post(ctx + "your/recall", { id: row.id });
            });
        }
    });
});
```

#### 步骤 5：改造详情页底部按钮

```html
<!-- 原来：手动拼接按钮 -->
<div class="box-footer">
    <button th:if="${paper.state == '1'}" class="btn btn-primary" onclick="approve()">通过</button>
    <button th:if="${paper.state == '1'}" class="btn btn-danger" onclick="reject()">驳回</button>
</div>

<!-- 改为：使用 PageRender 渲染 -->
<div class="box-footer" id="detail-actions">
    <!-- 按钮由 page-render.js 自动渲染 -->
</div>

<script th:inline="javascript">
$(function() {
    // 从后端获取 actions 数据
    var actions = [[${paper.actions}]];
    // 渲染详情页底部按钮
    PageRender.renderActions(actions, '#detail-actions', {
        view: function() { /* ... */ },
        approve: function() { /* ... */ },
        reject: function() { /* ... */ }
    });
});
</script>
```
