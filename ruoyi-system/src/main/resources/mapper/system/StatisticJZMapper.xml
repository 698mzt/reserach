<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.StatisticJZMapper">
    <select id="selectAllJZ" resultType="map">
        SELECT
            a.*
        FROM Alltotle a
        <where>
            <if test="deptId != null and deptId != ''">
                AND deptId = #{deptId}
            </if>
            <if test="userId != null">
                AND userId = #{userId}
            </if>
        </where>
    </select>

    <select id="selectYJCGSXY" resultType="map">
        SELECT
            a.*
        FROM Alltotle a
        WHERE partenId = #{parentId}
    </select>

    <select id="selectYJCGSXYByDeptId" resultType="map">
        SELECT a.*
        FROM Alltotle a
        WHERE a.deptId IN (
            SELECT d.dept_id FROM sys_dept d WHERE d.parent_id = #{deptId} AND d.del_flag = '0'
        )
    </select>

    <select id="selectDeptNameById" resultType="string">
        SELECT dept_name FROM sys_dept WHERE dept_id = #{deptId}
    </select>

    <select id="selectYJCGSKYC" resultType="map">
        SELECT a.* FROM Alltotle a
        WHERE a.deptId IN (
            SELECT d.dept_id FROM sys_dept d
            WHERE d.parent_id IN (
                SELECT dept_id FROM sys_dept WHERE parent_id = 100 AND dept_name LIKE '%学院%' AND del_flag = '0'
            )
            AND d.del_flag = '0'
        )
        <if test="userName != null and userName != ''">
            AND a.userName LIKE CONCAT('%', #{userName}, '%')
        </if>
    </select>

    <select id="selectYJCGSXX" resultType="map">
        SELECT a.* FROM Alltotle a
        WHERE a.deptId IN (
            SELECT d.dept_id FROM sys_dept d
            WHERE d.parent_id IN (
                SELECT dept_id FROM sys_dept WHERE parent_id = 100 AND dept_name LIKE '%学院%' AND del_flag = '0'
            )
            AND d.del_flag = '0'
        )
    </select>

    <select id="selectAllYJCGSKYC" resultType="map">
        SELECT a.* FROM Alltotle a
        <where>
            <if test="userName != null and userName != ''">
                AND a.userName LIKE CONCAT('%', #{userName}, '%')
            </if>
        </where>
    </select>
</mapper>